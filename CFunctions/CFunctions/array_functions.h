
int arr_min(int* numbers, int length);

int arr_max(int* numbers, int length);

int arr_clear(int* numbers, int length);

double arr_average(int* numbers, int length);

long arr_sum(int* numbers, int length);

int arr_contains(int* numbers, int length, int element);

int* arr_merge(int* array_one, int l1, int* array_two, int l2);
